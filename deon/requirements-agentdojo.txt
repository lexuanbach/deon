# Optional: dependencies for the public-benchmark validation on AgentDojo
# (agentdojo_deon.py). This is NOT needed for the deterministic core artifact
# (requirements.txt) or check_results.py; it reproduces the external head-to-head
# on the AgentDojo `banking` suite with a local Ollama proposer.
#
#   python3 -m venv .venv_ad && . .venv_ad/bin/activate
#   pip install -r requirements-agentdojo.txt
#   bash run_agentdojo.sh          # needs a local `ollama serve`
agentdojo>=0.1.35
openai>=1.0
# For the `pi_detector` baseline (ProtectAI DeBERTa-v3 prompt-injection classifier):
torch>=2.0
transformers>=4.40
