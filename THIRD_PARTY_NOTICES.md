# Third-party notices

NumPy, SciPy and Matplotlib are external dependencies of the model-free experiments and
keep their own licenses. The optional runs use Ollama and the six proposer models named
in `deon/run_ladder.sh`, `qwen2.5:7b`, AgentDojo, the OpenAI Python client, PyTorch,
Transformers, and four Hugging Face detector models named in `deon/agentdojo_deon.py`.
Each keeps its own license. This artifact redistributes no model weights and no
AgentDojo code. The result files name the models that were used. The two PDFs in
`paper/` keep their publication terms.
